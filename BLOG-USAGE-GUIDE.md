# 博客使用指南

## 📝 如何新写一篇文章

### 方法一：复制模板（推荐）

```bash
cd src/content/blog/llms/quantization/
cp template.mdx my-new-article.mdx
```

然后编辑 `my-new-article.mdx`：

```yaml
---
title: "你的文章标题"
description: "文章简短描述，约100字"
image: "../../blog_post.jpg"  # 或使用自己的封面图
publishDate: "2025-11-15"
tags: ["LLM", "Quant", "你的标签"]
---
```

### 方法二：从零开始

在 `src/content/blog/llms/quantization/` 目录下创建 `your-article.mdx`：

```markdown
---
title: "文章标题"
description: "文章描述"
image: "../../blog_post.jpg"
publishDate: "2025-11-15"
tags: ["标签1", "标签2"]
---

# 文章标题

你的内容...
```

---

## 🖼️ 如何添加封面图

### 选项一：使用项目默认图（最简单）

```yaml
image: "../../blog_post.jpg"
```

### 选项二：使用自己的封面图（推荐）

**1. 将封面图放到 images 文件夹**
```
src/content/blog/llms/quantization/images/
└── my-cover.png  ← 放这里
```

**2. 在文章中引用**
```yaml
image: "./images/my-cover.png"
```

**封面图建议尺寸**：1200x630 像素（适合社交媒体分享）

### 选项三：使用 public 目录

**1. 放到 public 目录**
```
public/images/blog/llms/quantization/
└── my-cover.png
```

**2. 在文章中引用**
```yaml
image: "/images/blog/llms/quantization/my-cover.png"
```

---

## 📷 如何在文章中插入图片

### 方法一：使用本地图片（推荐，自动优化）

**1. 把图片放到 images 文件夹**
```
src/content/blog/llms/quantization/images/
├── figure1.png
├── figure2.png
└── chart.jpg
```

**2. 在文章中插入**
```markdown
## 数据分析

下图展示了策略回测结果：

（注意：在实际文章中，使用标准的 markdown 图片语法，格式为：感叹号+方括号+圆括号）

路径：./images/figure1.png
说明：策略回测结果
```

**实际使用时的格式**：
- 感叹号 `!`
- 方括号中写描述 `[描述]`
- 圆括号中写路径 `(./images/图片名.png)`

### 方法二：使用 public 目录

**1. 把图片放到 public**
```
public/images/blog/llms/quantization/
└── figure1.png
```

**2. 在文章中引用**
```
路径：/images/blog/llms/quantization/figure1.png
```

### 图片命名建议
- ✅ 使用英文和数字：`figure1.png`, `chart-result.jpg`
- ✅ 使用连字符：`model-architecture.png`
- ❌ 避免中文：~~`图表1.png`~~
- ❌ 避免空格：~~`my chart.png`~~

---

## 🗑️ 如何删除一篇文章

### 完整删除步骤

**1. 删除文章文件**
```bash
# 在项目根目录执行
Remove-Item "src\content\blog\llms\quantization\your-article.mdx"
```

**2. 删除相关图片（如果有的话）**

如果你为这篇文章创建了专门的图片：

```bash
# 删除 images 文件夹中的相关图片
Remove-Item "src\content\blog\llms\quantization\images\your-*"

# 或删除 public 目录中的图片
Remove-Item "public\images\blog\llms\quantization\your-*"
```

**3. 刷新浏览器**

Astro 会自动检测文件变化，删除后刷新浏览器即可。

### 只需要删除什么

✅ **必须删除**：
- 文章 `.mdx` 文件

⚠️ **可选删除**：
- 该文章使用的封面图（如果是专门为它创建的）
- 该文章插入的图片（在 images/ 文件夹中）

❌ **不要删除**：
- 其他文章的文件
- 共享的封面图（如 `blog_post.jpg`）
- images 文件夹本身

---

## 📁 目录结构参考

```
src/content/blog/llms/quantization/
├── images/                    ← 图片文件夹
│   ├── .gitkeep              ← 保持文件夹存在
│   ├── article1-cover.png    ← 文章1的封面
│   ├── article1-fig1.png     ← 文章1的插图
│   └── article2-chart.jpg    ← 文章2的图表
├── template.mdx               ← 模板文件（不要删除）
├── getting-started.mdx        ← 示例文章（可删除）
├── test.mdx                   ← 测试文章（可删除）
└── your-article.mdx           ← 你的新文章
```

---

## 🎯 快速工作流

### 写新文章的流程

1. **复制模板**
   ```bash
   cd src/content/blog/llms/quantization/
   cp template.mdx 2025-11-15-my-article.mdx
   ```

2. **准备图片**
   - 封面图：1200x630 像素
   - 内容图片：适当压缩，建议小于 500KB
   - 放到 `images/` 文件夹

3. **编辑文章**
   - 修改 frontmatter（标题、描述、日期、标签）
   - 编写内容
   - 插入图片

4. **预览**
   ```bash
   npm run dev
   ```
   访问：http://localhost:4321/blog/llms/quantization/your-article

5. **发布**
   ```bash
   npm run build
   ```

---

## 💡 最佳实践

### 文章命名
- ✅ `llm-quant-strategy.mdx`
- ✅ `2025-11-15-market-analysis.mdx`
- ❌ `文章1.mdx`

### 标签使用
常用标签：
- `LLM` - 大语言模型
- `Quant` - 量化交易
- `Python` - Python 编程
- `Tutorial` - 教程
- `Research` - 研究

### 文件大小
- 文章文本：无限制
- 封面图：< 500KB
- 内容图片：< 300KB 每张
- 代码块：建议 < 50 行，太长可以提供 GitHub 链接

---

## ❓ 常见问题

### Q: 图片不显示？
A: 检查路径是否正确，相对路径从文章所在位置开始计算。

### Q: 可以使用中文文件名吗？
A: 不推荐，可能导致 URL 编码问题。

### Q: 如何修改已发布的文章？
A: 直接编辑 `.mdx` 文件，保存后刷新浏览器即可。

### Q: 删除文章后还显示？
A: 清理缓存并重启开发服务器：
```bash
Remove-Item -Path ".astro" -Recurse -Force
npm run dev
```

---

## 📞 获取帮助

- 查看 `template.mdx` - 完整功能示例
- 查看 `getting-started.mdx` - 详细写作指南
- Astro 文档：https://docs.astro.build
- MDX 文档：https://mdxjs.com

---

**最后更新**：2025-11-15
